/**
 * app/shift-coverage/page.tsx
 *
 * Σελίδα εβδομαδιαίας επισκόπησης βαρδιών (route: /shift-coverage)
 * Εμφανίζει τον πίνακα Shift Coverage + auto-assign λειτουργία.
 */

import { ShiftCoverage } from "@/components/features/ShiftCoverage";

export default function ShiftCoveragePage() {
  return <ShiftCoverage />;
}